[Unit]
Description=Frontend (Green)
After=network.target

[Service]
Type=simple
User={{ app_user }}
Group={{ app_group }}
WorkingDirectory={{ app_dir }}/green/packages/frontend
ExecStart=/usr/bin/serve -s dist -l {{ green_frontend_port }}
ExecStop=/bin/kill -SIGTERM $MAINPID
TimeoutStopSec=30
Restart=always
RestartSec=10

# Environment
Environment="NODE_ENV=production"
Environment="DEPLOYMENT_COLOR=green"

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=frontend-green

# Security
NoNewPrivileges=true
PrivateTmp=true

# Resource limits
LimitNOFILE={{ max_open_files }}
LimitNPROC={{ max_user_processes }}

# Memory limits
MemoryMax=1G
MemoryHigh=750M

[Install]
WantedBy=multi-user.target
